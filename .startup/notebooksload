#!/bin/bash
set -x
exec 2> /tmp/debugoutnotebooks
# create Cassandra Schema
if [ `hostname` == 'node0' ]
    then
        pushd /root/; tar -zxvf /tmp/SDDemos/notebooks.tar; popd
elif [ `hostname` == 'node1' ]
    then
        pushd /root/; tar -zxvf /tmp/SDDemos/notebooks.tar; popd
fi
